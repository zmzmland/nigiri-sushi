# チーム向け — GitHub のはじめかた

Git を触ったことがない人向けです。コマンドは使わず、**GitHub Desktop**
というアプリで操作します。

---

## 0. Git とは何か（3行で）

- **みんなで同じファイルを編集するための仕組み**です
- 誰がいつ何を変えたかが全部記録され、**壊しても前の状態に戻せます**
- 「保存」と「共有」が別々になっているのが最大の違いです

やることは、実質この3つだけです。

| 操作 | 意味 | いつやるか |
|---|---|---|
| **Pull** | みんなの変更を自分に取り込む | **作業を始める前に必ず** |
| **Commit** | 自分の変更に名前を付けて記録する | 区切りがついたとき |
| **Push** | 自分の記録をみんなに送る | Commit のあと |

**朝は Pull、帰りは Commit → Push。** これだけ覚えれば動けます。

---

## ⚠️ 最初に — Unity のバージョンを揃える

**このプロジェクトは Unity `6000.4.6f1` で作られています。全員これを使ってください。**

違うバージョンで開くと、Unity が「アップグレードしますか」と聞いてきます。
**ここで OK を押すと、シーンやアセットが書き換わります。** しかもその変更が
Git に乗って全員に伝わるので、1人のミスが全員に波及します。

Git 共有で最も事故が多いのがここです。

### 確認のしかた

1. Unity Hub を開く
2. 左の **Installs** タブを見る
3. `6000.4.6f1` があるか確認

無ければ **Install Editor** から探してインストールします。一覧に出てこない
場合は、下部の **Archive** → **download archive** から辿れます。

インストール時のモジュールは、**macOS Build Support (Mono)** にチェックを
入れてください。後でビルドするときに必要になります。

### プロジェクトを開くとき

Unity Hub の一覧で、プロジェクト名の横に表示されるバージョンが
`6000.4.6f1` になっていることを確認してから開いてください。
黄色い警告アイコンが出ていたら、バージョンが違っています。

**もし間違って開いてアップグレードしてしまったら、Commit も Push もせずに
すぐ相談してください。** Pull し直せば元に戻せます。

---

## 1. 全員が最初にやること

### GitHub のアカウントを作る

https://github.com にアクセスして、Sign up からアカウントを作ります。
ユーザー名は本名でなくて構いませんが、**チーム内で誰か分かる名前**にして
ください。後で「この変更は誰？」と辿ることになります。

作れたら、リポジトリを作る人にユーザー名を伝えてください。

### GitHub Desktop を入れる

https://desktop.github.com からダウンロードしてインストールします。
起動したら、上で作ったアカウントでサインインします。

---

## 2. 【1人だけ】リポジトリを作る

**代表者1人**が行います。他の3人は次の「3.」へ進んでください。

### アップロードできない状態を先に直す

**この作業を飛ばすと、途中でエラーになって全員が止まります。**

`Assets/TextMesh Pro/Noto_Sans_JP (1)/static/NotoSansJP-Light SDF.asset` が
**166MB** あります。GitHub は 100MB を超えるファイルを受け付けません。

1. Unity で Project ウィンドウからこのファイルを選択
2. Inspector の **Generation Settings** を開く
3. **Atlas Population Mode** を `Static` → **`Dynamic`** に変更
4. 下部の **Clear Dynamic Data** を押す
5. `Cmd + S`

これで数百KBになります。ゲームを Play して、文字が今までどおり表示される
ことを確認してください。

### リポジトリを作る

1. GitHub Desktop を開く
2. **File → Add Local Repository**
3. `My project` フォルダを選ぶ
4. 「このフォルダは Git リポジトリではありません」と出るので、
   **create a repository** のリンクを押す
5. 内容を確認して **Create Repository**

`.gitignore` は既にプロジェクトに置いてあるので、**Git ignore の欄は
`None` のまま**にしてください。上書きされると 3.7GB の `Library` フォルダが
アップロード対象になってしまいます。

### 最初の Commit と Push

1. 左側に変更ファイルの一覧が出ます。**数百件あるのが正常**です
2. **⚠️ 一覧に `Library` が含まれていないか確認してください。**
   含まれていたら `.gitignore` が効いていません。そのまま進めず相談を
3. 左下の **Summary** に `最初のコミット` と入力
4. **Commit to main** を押す
5. 上部の **Publish repository** を押す
6. 名前を決め、**Keep this code private にチェック**を入れて Publish

### メンバーを招待する

1. GitHub Desktop の **Repository → View on GitHub** でブラウザを開く
2. **Settings → Collaborators → Add people**
3. 3人のユーザー名を入力して招待

招待された人にはメールが届くので、承認してもらいます。

---

## 3. 【残り3人】自分のパソコンに持ってくる

1. 招待メールのリンクから承認する
2. GitHub Desktop で **File → Clone Repository**
3. 一覧からリポジトリを選ぶ
4. **Local Path** で保存先を選ぶ（デスクトップなどで構いません）
5. **Clone** を押す

### Unity で開く

1. Unity Hub を開く
2. **Add → Add project from disk**
3. clone したフォルダを選ぶ
4. 開く（**初回は `Library` の生成に5〜10分かかります**。固まったように
   見えても待ってください）

### 画像認識の準備

ターミナルを開いて、順に実行します。`<clone した場所>` は自分のパスに
読み替えてください。

```bash
cd "<clone した場所>/My project/ImageSearch"
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

5〜15分かかります。約2GB ダウンロードします。

続けて、自分のカメラを設定します。

```bash
cp config.example.json config.json
python3 camera_pick.py
```

iPhone の映像が出たら `y` を押してください。

**`config.json` は共有されません。** カメラ番号はパソコンごとに違うので、
各自が自分の環境で作ります。

---

## 4. 毎日の使いかた

### 作業を始める前 — Pull

GitHub Desktop の上部にある **Fetch origin** を押します。

`Pull origin` に変わったら押してください。**これで他の人の変更が
自分の手元に入ります。**

⚠️ **Pull する前に Unity を閉じておくと安全です。** 開いたままだと、
Unity が古い状態を掴んだまま上書きしてしまうことがあります。

### 作業する

いつもどおり Unity や VSCode で編集します。

### 作業が終わったら — Commit → Push

1. GitHub Desktop を開くと、変更したファイルが一覧に出ます
2. 左下の **Summary** に、何をしたか短く書きます
3. **Commit to main** を押す
4. 上部の **Push origin** を押す

Push するまで、変更は自分のパソコンの中だけにあります。**Push を忘れると
誰にも届きません。**

### Summary の書き方

後から探すときに効くので、**何を変えたか**を書いてください。

```
◯ リザルト画面にタイトルへ戻るボタンを追加
◯ 判定ロジックを LCS に変更
◯ たこのスプライトを追加

× 更新
× 修正
× aaa
```

---

## 5. ⚠️ 絶対に守ってほしいルール

### シーンファイルは1人ずつ

`Assets/Scenes/*.unity` は、**2人が同時に編集するとほぼ確実に壊れます。**
テキストではあるものの、人間が読んで直せる形式ではないためです。

**シーンを触る前に、チャットで「Game Scene 触ります」と宣言してください。**
終わったら「終わりました」と伝えます。地味ですが、これが一番確実です。

同じことがこれらにも当てはまります。

- `Assets/Scenes/*.unity`
- `ProjectSettings/*`（Build Settings や Player Settings）

スクリプト（`.cs`）や Python（`.py`）は、**別の関数を触っている限り**
同時編集しても Git が自動で解決してくれます。

### Library フォルダを Commit しない

3.7GB あります。`.gitignore` で除外していますが、もし変更一覧に
`Library` が出てきたら、**Commit せずに相談してください。**

### Pull せずに作業を始めない

古い状態から作業すると、後で衝突します。**朝いちばんに Pull**。

---

## 6. 困ったとき

### 「Conflict（衝突）」と出た

同じ場所を2人が変えた状態です。**自分で解決しようとせず、まず相談して
ください。** 特にシーンファイルの衝突は、片方の変更を捨てて作業をやり直す
方が早いことがほとんどです。

### 間違えて Commit した

まだ Push していなければ、GitHub Desktop の **History** タブから該当の
コミットを右クリック → **Undo commit** で取り消せます。

Push 済みの場合も戻せますが、他の人に影響するので相談してください。

### Unity を開いたらシーンが真っ白 / エラーだらけ

`Library` の再生成が途中の可能性があります。Unity を閉じて開き直して
ください。それでも直らなければ、`Library` フォルダを削除して開き直すと
作り直されます（時間はかかりますが安全です）。

### Push できない（rejected と出る）

他の人が先に Push しています。**先に Pull してから** Push し直してください。

---

## 7. 用語の対応表

| 見かける言葉 | 意味 |
|---|---|
| リポジトリ | プロジェクトのフォルダ全体 |
| Clone | リポジトリを自分のパソコンにコピーする |
| Commit | 変更に名前を付けて記録する（まだ共有されない） |
| Push | 記録を GitHub に送る（ここで共有される） |
| Pull | GitHub から最新を取り込む |
| Fetch | 更新があるか確認だけする |
| main | 全員が使う基本の作業場所 |
| Conflict | 同じ場所を2人が変えて、Git が判断できない状態 |


---

## 8. 役割分担について

このゲームは **iPhone の連係カメラ**を使うので、**展示で動くのは1台だけ**です。
Mac と iPhone が同じ Apple ID である必要があるためです。

そのため、次のように分けます。

| | やること |
|---|---|
| 展示機の担当（1人） | 最新を Pull して、展示機でビルドする |
| ほかの3人 | Unity Editor で Play して確認する。**ビルドは不要** |

**各自でビルドする必要はありません。** ビルドは環境に依存する作業なので、
1台に固定するのが最も確実です。機能の確認は Editor で全てできます。

### 保険

ビルド担当が展示当日に来られないと詰みます。**別の1人が、一度だけ練習で
ビルドを通してみてください。** 手順は `ImageSearch/dist_files/アプリ化手順.md`
に全部書いてあります。1時間ほどで終わり、手順書の不備もそこで見つかります。
